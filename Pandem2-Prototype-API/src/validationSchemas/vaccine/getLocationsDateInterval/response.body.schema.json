{
  "$id": "vaccine.getLocationsDateInterval.response.body.schema",
  "description": "Get time interval for vaccines for requested locations response body schema",
  "type": "object",
  "properties": {
    "start_date": {
      "type": "string",
      "format": "date"
    },
    "end_date": {
      "type": "string",
      "format": "date"
    }
  }
}
