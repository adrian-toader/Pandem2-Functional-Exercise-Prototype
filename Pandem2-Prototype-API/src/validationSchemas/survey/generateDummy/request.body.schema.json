{
  "$id": "survey.generateDummy.request.body.schema",
  "description": "Generate dummy data for surveys",
  "type": "object",
  "properties": {
    "location": {
      "$ref": "/surveyAnswer#/definitions/location"
    },
    "generate_for_sublocations": {
      "type": "boolean"
    },
    "start_date": {
      "type": "string",
      "format": "date"
    },
    "end_date": {
      "type": "string",
      "format": "date"
    }
  },
  "required": [
    "location",
    "start_date",
    "end_date"
  ]
}
